% ==========================================================
% MACROS / COMMANDS (FRF / Electric Barometer)
% LTO Paper: Readiness-Centric LTO Management
% ==========================================================

% ----------------------------------------------------------
% CORE SERIES / FRAMEWORK TERMS
% ----------------------------------------------------------

% Forecast Readiness Framework
\newcommand{\FRF}{\ensuremath{\mathrm{FRF}}}

% Readiness Adjustment Layer
\newcommand{\RAL}{\ensuremath{\mathrm{RAL}}}

% Limited-Time Offer(s)
\newcommand{\LTO}{\ensuremath{\mathrm{LTO}}}
\newcommand{\LTOs}{\ensuremath{\mathrm{LTOs}}}

% ----------------------------------------------------------
% KEY METRICS (as referenced in paper)
% ----------------------------------------------------------

% No-Shortfall Level
\newcommand{\NSL}{\ensuremath{\mathrm{NSL}}}

% Cost-Weighted Service Loss
\newcommand{\CWSL}{\ensuremath{\mathrm{CWSL}}}

% ----------------------------------------------------------
% FORECAST / DEMAND NOTATION
% ----------------------------------------------------------

% Observed demand and point forecast (consistent across EB papers)
\newcommand{\y}{\ensuremath{y}}
\newcommand{\yhat}{\ensuremath{\hat{y}}}

% Under- and over- error components (signed)
\newcommand{\underErr}{\ensuremath{(y_t - \hat{y}_t)}}
\newcommand{\overErr}{\ensuremath{(\hat{y}_t - y_t)}}

% ----------------------------------------------------------
% OPERATORS
% ----------------------------------------------------------

% Positive-part operator: (x)_+ = max(x, 0)
\newcommand{\pospart}[1]{\left(#1\right)_{+}}

% Indicator function
\newcommand{\indicator}[1]{\ensuremath{\mathbf{1}\{#1\}}}

% ----------------------------------------------------------
% COST / RISK PARAMETERS (asymmetry)
% ----------------------------------------------------------

% Underbuild and overbuild cost weights (generic)
\newcommand{\cu}{\ensuremath{c_u}}
\newcommand{\co}{\ensuremath{c_o}}

% ----------------------------------------------------------
% LTO CONTEXT / REGIME FLAGS (paper-specific)
% ----------------------------------------------------------

% Binary LTO activation flag
\newcommand{\ltoOn}{\ensuremath{z_t}}

% LTO phase label (launch/sustain/wind-down), treated as categorical context
\newcommand{\ltoPhase}{\ensuremath{\phi_t}}