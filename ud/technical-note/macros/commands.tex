% ----------------------------------------------------------
% METRIC MACROS (FRF / Electric Barometer)
% ----------------------------------------------------------

% Metric name (typeset as upright math)
\newcommand{\UD}{\ensuremath{\mathrm{UD}}}

% ----------------------------------------------------------
% FORECAST / DEMAND NOTATION
% ----------------------------------------------------------

% Forecast hat (consistent across notes)
\newcommand{\yhat}{\hat{y}}

% ----------------------------------------------------------
% OPERATORS
% ----------------------------------------------------------

% Positive part operator: (x)_+ = max(x, 0)
\newcommand{\pospart}[1]{\left(#1\right)_{+}}

% Shortfall depth symbol (optional convenience)
\newcommand{\sdepth}{s}